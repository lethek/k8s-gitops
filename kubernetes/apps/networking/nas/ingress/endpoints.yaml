---
apiVersion: v1
kind: Endpoints
metadata:
  name: nas
  namespace: networking
subsets:
  - addresses:
      - ip: ${EXTERNAL_NAS_ADDR}
    ports:
      - name: https
        port: 5001
        protocol: TCP
